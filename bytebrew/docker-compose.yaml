services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: bytebrew
      POSTGRES_USER: bytebrew
      POSTGRES_PASSWORD: bytebrew
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U bytebrew"]
      interval: 5s
      timeout: 5s
      retries: 5

  engine:
    build:
      context: ./engine
      dockerfile: Dockerfile
    ports:
      - "8443:8443"
    environment:
      - BYTEBREW_ADMIN_USER=admin
      - BYTEBREW_ADMIN_PASSWORD=changeme
      - DB_USER=bytebrew
      - DB_PASSWORD=bytebrew
    depends_on:
      postgres:
        condition: service_healthy

volumes:
  pgdata:
